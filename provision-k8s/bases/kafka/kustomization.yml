apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  tracegrapher-tier: kafka-broker

namespace: trace-grapher

resources:
  - kafka-cluster.yml
  - kafka-connect.yml
  - connect-ui-deployment.yml
  - connect-ui-service.yml
  - kafka-topic.yml
