apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring-stack

bases:
  - alertmanager
  - fluentd
  - node-exporter
  - grafana
  - jaeger-tracing
  - kafka
  - kube-state-metrics
  - metric-server
  - opentelemetry
  - prometheus
  - prometheus-adapter

resources:
  - gateway.yml
  - tracing-lb.yml
